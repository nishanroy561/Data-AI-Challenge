# Pin versions before submitting (run: pip freeze | grep -i <pkg>)
numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0           # parquet I/O for precomputed features
scikit-learn==1.5.0       # cosine / normalization helpers (CPU)

# ---- precompute only (NOT imported by rank.py) ----
# rank.py must stay offline + model-free; these are used in precompute.py
sentence-transformers==3.0.1
torch==2.3.0              # CPU build is fine; GPU used only in precompute
