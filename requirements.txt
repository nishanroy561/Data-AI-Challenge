# Dependencies for the Redrob ranker (pinned versions).
# The offline ranking step (rank.py) imports ONLY numpy + the Python stdlib —
# so Stage-3 reproduction needs nothing heavy. Everything below numpy is
# precompute-only and is NOT imported by rank.py.
numpy==1.26.4

# ---- precompute only (NOT imported by rank.py) ----
# precompute.py embeds candidate narratives with a local sentence-transformer;
# rank.py stays model-free and fully offline.
sentence-transformers==3.0.1
torch==2.3.0              # required by sentence-transformers; GPU used only in precompute
